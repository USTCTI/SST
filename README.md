# SST
SST(System Stress Test),A single-file tool for aggressive stress testing on a local machine, covering CPU, Memory, Network (UDP), and Public Bandwidth (download) tests. It automatically prints a summary report after each run, making it easy to compare system behavior under different scenarios.
